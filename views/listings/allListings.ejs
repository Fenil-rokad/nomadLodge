<% layout(`layouts/boilerplate`) %>

    <body>
        <div class="container">
            <ul>
                <% for(listing of allListings){ %>
                    <li>
                        <a href="/listings/<%= listing._id %>">
                            <%= listing.title %>
                        </a>
                    </li>
                    <% } %>
            </ul>
        </div>
    </body>